# Legacy compiled artifacts

Pre-v3 hand-crafted experience library and other artifacts not used by the v3 GEPA + TF-GRPO workflow.

| File | What it is |
|---|---|
| `strat100_E_manual.txt` | Hand-written experience library used during early MuSiQue strat100 sweeps |
| `strat100_E_manual.json` | Same library in structured form |

Current v3 artifacts in the parent directory:
- `gepa_v3_recovered_cand13.json` — recovered GEPA winner (planner / synthesizer / critic prompts)
- `oracle/<dataset>_fresh_naive/predictions.jsonl` — SAS-mode oracle on fresh train pool
